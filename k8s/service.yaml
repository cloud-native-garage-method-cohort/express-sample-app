apiVersion: v1
kind: Service
metadata:
  labels:
    app: poornima-ta-live-pfs
    app.kubernetes.io/instance: poornima-ta-live-pfs
    app.kubernetes.io/name: poornima-ta-live-pfs
  name: poornima-ta-live-pfs
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 3000
  selector:
    app.kubernetes.io/instance: poornima-ta-live-pfs
    app.kubernetes.io/name: poornima-ta-live-pfs
  sessionAffinity: None
  type: ClusterIP